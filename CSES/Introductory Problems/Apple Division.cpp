#include <bits/stdc++.h>
using namespace std;
#define fast ios::sync_with_stdio(false); cin.tie(nullptr); cout.tie(nullptr);
#define ll long long

int helper(vector<int>& apples, const int& n){
    
}

int main() {
    fast;
    int n;
    cin>>n;
    vector<int> apples(n);
    for(auto& i : apples) cin>>i;
    cout<<helper(apples, n)<<endl;
    return 0;
}