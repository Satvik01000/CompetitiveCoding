#include <bits/stdc++.h>
using namespace std;
#define fast ios::sync_with_stdio(false); cin.tie(nullptr); cout.tie(nullptr);
#define ll long long

int sandFallAfterLeave(int s, int k, int m){
    
}

int main() {
    fast;
    int t;
    cin >> t;
    while (t--) {
        int s, k, m;
        cin>>s>>k>>m;
        cout<<sandFallAfterLeave(s, k, n)<<endl;
    }
    return 0;
}
